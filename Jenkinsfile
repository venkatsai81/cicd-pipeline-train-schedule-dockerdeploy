pipeline {
    agent any
    stages {
        stage('Build') {
           
              steps {
                echo 'oneoneoneone'
                //sh './gradlew build --no-daemon'
                //archiveArtifacts artifacts: 'dist/trainSchedule.zip'
            }
        }
         stage('compile') {
           
              steps {
                echo 'oneoneoneone'
                //sh './gradlew build --no-daemon'
                //archiveArtifacts artifacts: 'dist/trainSchedule.zip'
            }
        }
    }
}
