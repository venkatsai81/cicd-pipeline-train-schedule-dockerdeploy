pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               echo 'twooooooooootwo'
                //sh './gradlew build --no-daemon'
                //archiveArtifacts artifacts: 'dist/trainSchedule.zip'
            }
              steps {
                echo 'oneoneoneone'
                //sh './gradlew build --no-daemon'
                //archiveArtifacts artifacts: 'dist/trainSchedule.zip'
            }
        }
    }
}
